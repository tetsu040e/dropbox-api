use strict;
use warnings;
use utf8;
use File::Basename qw(dirname);

return +{
    dropbox => +{
        app_key      => 'XXXXXXX',
        app_secret   => 'XXXXXXX',
        redirect_uri => 'https://example.com/path/to/redirect',
    },
};
